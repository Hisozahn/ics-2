//////////////////////// Main ////////////////////////////////
// Главная функция
//////////////////////////////////////////////////////////////
#include "anim.h"
#include "sys_timer.h"
#include "led.h"

void main( void )
{
	InitTimer();
	while( 1 ) {
		//anim_m();
        //DelayMs(300);
        /*leds(0x55);
		DelayMs(300);
        leds(~0x55);
		DelayMs(300);*/
		
		//delay(3);
		//anim_m();
		//delay(3);
	}
}