//////////////////////// Main ////////////////////////////////
// Главная функция
//////////////////////////////////////////////////////////////
#include "anim.h"
#include "sys_timer.h"

void main( void )
{
	InitTimer();
	while( 1 ) {
		anim_m();
		DelayMs(500);
		//delay(3);
		//anim_m();
		//delay(3);
	}
}